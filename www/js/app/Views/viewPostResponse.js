/*define(['app/src/controllers/Task1', 'jquery', 'handlebars'], function(T, $, Handlebars) {
	function render(returnValue) {
		console.log('rener Task1 run');
		var html = '<div>{{responseText}}</div>';
		//returnValue = {name: 'lolka'}
		var template = Handlebars.compile(html);
		$('#container').append(template(returnValue));
		
	} 	
	return {
		render: render
	};
});*/
